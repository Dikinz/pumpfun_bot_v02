# For free new-token discovery, leave empty first.
# If PumpPortal requires a key for your connection, add it here.
PUMPPORTAL_API_KEY=

# false = only free new-token stream
# true = allows per-token trade subscriptions (metered by PumpPortal)
ENABLE_METERED_TRADE_STREAM=false

DATABASE_PATH=data/trades.db
